name: Historico Inventarios - Ejecución Manual

on:
  workflow_dispatch:  # Solo ejecución manual

env:
  API_TOKEN: ${{ secrets.API_TOKEN }}
  API_BASE_URL: ${{ secrets.API_BASE_URL }}

jobs:
  collect-data:
    runs-on: ubuntu-latest  # Usar la última versión para mejor rendimiento
    timeout-minutes: 45  # Reducido porque debería ser más rápido

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install pandas requests

    - name: Run API collector
      run: python api_collector.py

    - name: List output files
      run: |
        echo "📂 Archivos generados:"
        ls -lh data/
        echo "📊 Tamaño de archivos:"
        du -h data/*

    - name: Upload CSV as artifact
      uses: actions/upload-artifact@v4
      with:
        name: historico-inventarios-csv
        path: data/*
        retention-days: 2

    - name: Clean up files
      run: |
        # Limpiar archivos temporales
        rm -f debug_*.txt debug_*.json
